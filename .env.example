# Backend (FastAPI) — src/core/config.py
ENVIRONMENT=development
HOST=127.0.0.1
PORT=8001
CORS_ORIGINS=["http://localhost:3002"]

# ---- Voice (docs/11-voice.md) ---------------------------------------------
# Sarvam does the hearing (Saaras) and the speaking (Bulbul), and answers too
# when OpenAI is absent. One key is enough to hold a conversation.
SARVAM_API_KEY=

# Optional. With it set, OpenAI writes the replies and covers every language
# Bulbul cannot speak — French, Japanese, anything off the Indic list.
OPENAI_API_KEY=

# Override either half explicitly. Left empty they resolve from the keys above:
# OpenAI first, Sarvam second.
# LLM_BASE_URL=https://api.sarvam.ai/v1
# LLM_MODEL=sarvam-105b-conversations

# The voice. Speakers are model-specific — bulbul:v3 rejects bulbul:v2's names.
TTS_MODEL=bulbul:v3
TTS_SPEAKER=ritu

# Retrieval, off. The RAG pipeline below is built and measured (docs/09-v1.md);
# with this false the spoken turn answers conversationally, boot skips the ONNX
# session and the index, and /ask abstains rather than pretending.
RAG_ENABLED=false

# ---- Retrieval (only read when RAG_ENABLED=true) ---------------------------
# Vector store. Empty QDRANT_URL runs Qdrant in-process over QDRANT_PATH, which
# is a single-writer lock: stop the API before running scripts/ingest.py. Point
# this at `docker run -p 6333:6333 qdrant/qdrant` to ingest and serve together.
QDRANT_URL=
QDRANT_PATH=data/qdrant
QDRANT_COLLECTION=vec-chunks

# Embedding — local ONNX, no network call on the answer path.
EMBED_MODEL=intfloat/multilingual-e5-small
EMBED_CACHE_DIR=data/models

# Guardrails (docs/06-guardrails.md). Gate 2. The margin is the knob that moves:
# 0.010 answers more (coverage 0.75, catches 34% of unanswerable), 0.030 abstains
# more (coverage 0.29, catches 90%). Swept in docs/09-v1.md; re-sweep per index.
RETRIEVAL_FLOOR=0.845
RETRIEVAL_MARGIN=0.02

# Requirement 3's budget, in ms. The harness degrades rather than overrunning it.
DEADLINE_MS=200

# Keep the ONNX session and the vector matrix hot between questions. After 30 s
# idle a cold answered request measured 200 ms; with this it is ~92 ms. Costs one
# tiny embed + search per tick. 0 disables it.
KEEPALIVE_SECONDS=20
