# Dev/test dependencies, fully pinned. Generated from uv.lock — do not hand-edit:
# edit the `dev` dependency-group in pyproject.toml, run `uv lock`, then regenerate
# the tail of this file with
#
#     uv export --frozen --only-dev --no-emit-project --no-hashes --no-header \
#       --format requirements-txt
#
# Installing this installs the runtime set too.

-r requirements.txt

colorama==0.4.6 ; sys_platform == 'win32'
    # via pytest
iniconfig==2.3.0
    # via pytest
packaging==26.3
    # via pytest
pluggy==1.6.0
    # via pytest
pygments==2.21.0
    # via pytest
pytest==9.1.1
